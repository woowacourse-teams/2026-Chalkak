# 사진 변환 처리 설계 대화의 맥락

이미지 변환을 요청 안에서 끝내면 구현이 단순하고 성공·실패를 바로 응답할 수 있다. 작업 큐를 쓰면 요청 대기를 줄일 여지가 있지만 처리 상태 조회와 중복 작업 방지가 필요하다.

처음에는 업로드 빈도가 낮아 동기 처리를 우선 검토했다. 이어서 사진 한 장의 변환 시간이 불안정할 때 요청 타임아웃과 사용자 재시도가 겹칠 수 있다는 위험을 논의했다. 큐와 중복 방지 저장소가 아직 없어서 운영 부담도 함께 따져야 한다. 지연 시간을 측정한 결과는 없으며 설계에 대한 대화만 했다.
