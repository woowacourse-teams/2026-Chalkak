---
name: main-code
description: backend/src/main/java의 Java 운영 코드를 생성·수정·리뷰할 때 사용한다. 테스트 코드와 client 코드에는 사용하지 않는다.
---

# 운영 코드 컨벤션

## 적용 방법

1. 변경 대상이 `src/main/java`인지 확인한다.
2. 새로 작성하거나 수정한 코드에 아래 규칙을 적용한다.
3. 관련 없는 기존 코드는 컨벤션 적용을 이유로 변경하지 않는다.
4. 완료 전 변경한 코드를 아래 규칙으로 다시 확인한다.

## 기본 스타일

- Google Java Style을 기본으로 한다.
- 이 문서의 팀 규칙과 충돌하면 팀 규칙을 우선한다.
- 블록 들여쓰기는 공백 4칸을 사용한다.
- 줄바꿈 후 계속되는 줄은 기존 줄보다 최소 8칸 들여쓴다.
- UTF-8·LF를 사용하고 파일 끝 개행을 추가하며 줄 끝 공백을 제거한다.
- 완료 전 변경한 파일에서 사용하지 않는 import를 제거한다.

## 괄호와 블록

- `if`, `for`, `while`은 한 줄이어도 중괄호를 사용한다.
- `else`, `else if`, `switch`를 사용하지 않는다.
- 조건 분기는 단독 `if`와 Early Return으로 작성한다.
- 여는 중괄호는 선언문과 같은 줄에 둔다.
- 빈 블록을 작성하지 않는다.
- 매개변수가 3개 이상이면 매개변수마다 줄을 나눈다.

```java
void get(
        Long first,
        Long second,
        Long third
) {
    process(first, second, third);
}
```

## 검증 로직 재사용

- 동일한 도메인 규칙을 검증하는 기존 메서드를 우선 재사용한다.
- 메시지나 행위만 다르고 변경 이유가 같은 중복 로직은 `private` 공통 메서드로 추출한다.
- 상태별 예외나 메시지처럼 정책이 다른 로직은 무리하게 통합하지 않는다.

## 상수

- 파일 내부에서 추출한 상수는 `private`으로 선언하고, `public`으로 노출하지 않는다.

## 클래스 내부 배치 순서

1. 상수
2. 필드
3. 생성자
4. `public` 메서드
5. `protected` 메서드
6. `private` 메서드
7. getter와 setter
8. `hashCode()`
9. `equals()`
