# Chalkak 공통 지침

## 비즈니스 규칙

- 서비스 정책, 여러 클라이언트가 의존하는 도메인 동작 또는 API 의미를 기획·구현하거나 변경할 때는 `business-rules` Skill을 사용한다.
- 공통 규칙은 `docs/business-rules/README.md`에서 필요한 도메인 문서만 찾아 읽는다. 모든 규칙과 변경 기록을 한꺼번에 읽지 않는다.
- `backend/**` 작업은 사용자와 합의한 정책을 규칙 문서에 반영할 수 있다. `client/**` 작업은 규칙 문서를 읽기 전용으로 사용하고 현재 규칙에 맞춰 구현하며 `docs/business-rules/**`를 수정하지 않는다.
- client 작업에서 규칙이 명확하면 추가 확인 없이 구현한다. 규칙이 없거나 API·서버 동작과 충돌하면 해당 쟁점을 백엔드 확인 대상으로 알리고 임의로 정책을 만들지 않는다. 정책 변경을 제안할 때도 공통 배포본을 직접 수정하지 않고 스킬의 백엔드 협의·재배포 절차를 따른다.
- 현재 규칙이나 결정 기록을 포함한 PR의 등록 흐름은 해당 스킬의 규칙 대조와 Notion 수동 반영 안내까지 연결한다. 템플릿·동기화 설정만 바뀐 PR은 대상이 아니다.

- 클라이언트의 정책 관련 작업 시작·재개에는 공통 스킬의 버전 확인·관련 규칙 읽기·적용 내용 안내 절차를 따른다. 공통 하네스 설치본은 읽기 전용이며, 원격 최신 확인과 서버 배포 여부를 구분한다.
- 백엔드에서 현재 규칙을 바꾸면 Android 팀에 사람이 전달할 변경·영향·적용 상태 문구도 작성한다. 자동 발송하지 않는다.
